# This file is just to make it easier to build/push the prod images

version: "3"

services:
  api:
    build: ./api/
    image: lucaspickering/krahv-api:latest

  static:
    build: ./webapp/
    image: lucaspickering/krahv-static:latest
